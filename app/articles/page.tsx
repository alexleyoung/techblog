import Link from "next/link";

const Articles = () => {
  // data = getData
  // data.map((post)=>{
  //   return <Article post={post} />
  // })

  return (
    <div>
      {/* {data.map((post) => {
        return <Article post={post} />;
      })} */}
    </div>
  );
};

// function article({ post }) {
//   return <Link href={`/articles/${post.title_slug}`}>{post.title}</Link>;
// }

export default Articles;
